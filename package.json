{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch contacts.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18"
  }
}
